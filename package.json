{
  "name": "fair-hours",
  "version": "0.1.0",
  "description": "See whose local time is quietly worst across a full year of a recurring meeting, DST included.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "Charlie Krug",
  "repository": {
    "type": "git",
    "url": "https://github.com/ctkrug/fair-hours.git"
  },
  "scripts": {
    "test": "node --test test/",
    "lint": "for f in site/js/main.js site/js/core/*.js site/js/ui/*.js; do node --check \"$f\" || exit 1; done"
  },
  "engines": {
    "node": ">=18"
  }
}
